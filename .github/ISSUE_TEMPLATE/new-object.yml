name: New object
description: Add a BASE object and its exit(s) to the OpenExits Commons without touching git
title: "[object] "
labels: ["new-object", "needs-review"]
body:
  - type: markdown
    attributes:
      value: |
        ## ⚠ Read before submitting
        **Do not submit access-restricted or landowner-sensitive exits.** An issue is
        public and permanent even if closed — see
        [SENSITIVE-EXITS.md](../blob/main/SENSITIVE-EXITS.md). The public Commons holds
        openly-known objects only.

        All measurements in **metres**, directions in **degrees true**.
  - type: input
    id: name
    attributes:
      label: Object name
      description: The object itself (the cliff, the bridge, the antenna, the building), not one exit
    validations:
      required: true
  - type: input
    id: country
    attributes:
      label: Country (two-letter code, e.g. FR, CH, IT)
    validations:
      required: true
  - type: input
    id: coordinates
    attributes:
      label: Exit coordinates (lat, lon — WGS84 decimal degrees)
      placeholder: "45.812345, 6.423456"
    validations:
      required: true
  - type: dropdown
    id: objectType
    attributes:
      label: Object type (the B.A.S.E. category of the object)
      options: [earth, building, antenna, span]
    validations:
      required: true
  - type: checkboxes
    id: suitability
    attributes:
      label: What can be jumped here?
      options:
        - label: slick (no wingsuit/tracksuit needed)
        - label: slider off
        - label: slider up
        - label: wingsuit
        - label: tracksuit
        - label: static line
  - type: input
    id: exitDirection
    attributes:
      label: Exit direction (degrees TRUE, 0-359)
  - type: input
    id: rockdrop
    attributes:
      label: Rockdrop (metres) + how measured + when
      placeholder: "220 m, laser, 2026-06"
  - type: input
    id: landing
    attributes:
      label: Landing coordinates (lat, lon) + surface
      placeholder: "45.814903, 6.428100 — grass"
  - type: textarea
    id: guide
    attributes:
      label: Access / approach / hazards (free text, any language)
  - type: dropdown
    id: access
    attributes:
      label: Legal status of access
      options: [legal, tolerated, restricted-seasonal, illegal, unknown]
    validations:
      required: true
  - type: checkboxes
    id: terms
    attributes:
      label: Contributor Terms (contributor-terms-2026-08)
      options:
        - label: >-
            I certify that I have the right to contribute this material and that it is
            not copied from a third-party database without permission. I keep my rights
            in my contribution, and I grant the OpenExits project — and the non-profit
            association that will be constituted to steward it, which succeeds to these
            rights automatically upon its creation — a worldwide, non-exclusive,
            irrevocable licence to publish it as part of the OpenExits database under
            ODbL 1.0 (contents under DbCL 1.0), or under another free and open licence
            approved by a two-thirds vote of active contributors.
          required: true
        - label: This object is openly known — not access-restricted or landowner-sensitive.
          required: true
