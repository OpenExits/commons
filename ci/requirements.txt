# The gate toolchain is versioned data: the validator is pinned to a
# specification release tag, so a gate result is reproducible for as long as
# that tag exists. Never track main here — a spec change would silently alter
# what CI accepts.
#
# Local development installs the editable package instead:
#   pip install -e ../specification/tools
openexits-validator @ git+https://github.com/OpenExits/specification@v2.0.2#subdirectory=tools
jsonschema>=4.21
