# The gate toolchain is versioned data: pin the validator to a specification
# release tag. INERT FORM until the openexits org exists — local development
# installs the editable package instead:
#   pip install -e ../specification/tools
#
# openexits-validator @ git+https://github.com/openexits/specification@v2.0.0#subdirectory=tools
jsonschema>=4.21
